services:
  - type: web
    name: durare-model
    runtime: docker
    plan: free
    rootDir: model-service
    healthCheckPath: /health
    envVars:
      - key: API_KEY
        sync: false
      - key: MODEL_LAYOUT
        value: multiquantile
      - key: MODEL_VERSION
        value: v1
      - key: SAFETY_BUFFER_DAYS
        value: "2"